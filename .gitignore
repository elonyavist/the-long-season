# Dependencies
node_modules/

# Build outputs
dist/
build/
coverage/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files
.env
.env.*
!.env.example

# Editor and OS files
.DS_Store
Thumbs.db
.idea/
.vscode/

# Package manager caches
.npm/
.pnpm-store/

# Future tooling caches
.turbo/
.vite/
.cache/

# graphify knowledge graph: derived output, rebuilt from source at no API cost.
# Upstream suggests committing it so a team shares one map; this repo has one
# author, so tracking it only ever bought a large diff per commit and once left
# 104 MB of AST cache in the history.
graphify-out/

# Tauri/Rust future outputs
target/

# Local game/runtime data
saves/
apps/cli/saves/
*.save.json
