{
  "name": "the-long-season",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": "24.16.0"
  },
  "scripts": {
    "test": "node -e \"console.log('No tests configured yet')\"",
    "lint": "node -e \"console.log('No lint configured yet')\"",
    "depcruise": "node -e \"console.log('No dependency rules configured yet')\"",
    "check": "pnpm lint && pnpm depcruise && pnpm test",
    "cli": "pnpm --filter @game/cli run cli --"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
