/**
 * CLI application entrypoint.
 *
 * The real command surface is added in later foundation steps. This file stays
 * intentionally empty in the monorepo skeleton so the package can typecheck
 * without introducing CLI behavior before `pnpm cli doctor`.
 */
export {};
