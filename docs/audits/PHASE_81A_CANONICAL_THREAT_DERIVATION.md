# Phase 81A Canonical Threat Derivation

## Decision

The ownership correction is accepted with no gameplay change. Phase 1 now
ranks responses with the same possession-to-chance and relative route-pressure
derivations consumed by the minute loop. The superseded absolute-saturation
threat formula no longer exists.

The current-materiality checkpoint remains `REFINE`. This is no longer
evidence against the analytic model: after alignment, both independent seed
sets retain complete response diversity, conservation, mirror coherence and
material cycles. The remaining owner is downstream of analytic response
ranking.

## Population And Result

The locked `phase81a-b2-current-materiality` profile ran alone with exactly
seven workers over Checkpoint A's seven in-sample worlds and seven untouched
out-of-sample worlds. Each set contains `378` real formation matchups expanded
over nine responses (`3,402` directed contexts), followed by the frozen
complete-row replay sample.

| fact | in-sample | out-of-sample | gate |
|---|---:|---:|---:|
| effective response signatures | `9/9` | `9/9` | complete |
| best-response signatures | `6/9` | `6/9` | `>= 0.25` share |
| ubiquity multiple | `3.8228` | `3.6111` | `<= 4` |
| material cycles | `148` | `147` | `> 0` |
| conservation mismatches | `0` | `0` | `0` |
| mirror mismatches | `0` | `0` | `0` |
| dominant responses | `0` | `0` | `0` |
| optimistic ceiling | `+0.02441` | `+0.02519` | `>= +0.045` |
| optimistic exposure | `-0.02053` | `-0.02159` | `<= -0.045` |
| blind delta | `+0.00021` | `-0.00379` | neutral |

All `21/21` population rows pass in each set. The profile returned real exit
`1`, decision `REFINE`, and wrote
`simulation-out/phase81a-b2-canonical-threat.json` with SHA-256
`fa65904c8c9a4b0ec65e882c3156d44790b775fadd17fe4b6b36e5d71877589d`.

## What Changed

- `OpportunityRoutePlan` carries the already-versioned route-capacity
  separation and possession influence needed by pure consumers.
- one engine helper owns possession share to chance creation;
- one engine helper owns relative route pressure and the pre-clamp opportunity
  rate in the exact production multiplication order;
- the minute loop and Phase 1 both call those helpers;
- analytic component vocabulary now says `route_pressure`, and its
  reconciliation reads the same multiplication order.

No calibration number, seed, action, formation or response changed.

## Handoff

The three coefficient families and the analytic/minute ownership split are
exhausted. The next measurement must retain complete-row telemetry for chance
frequency, expected goals and final result. It must determine whether tactical
responses fail to create enough xG separation, or whether material xG is being
diluted only when resolved into goals and win share. No further coefficient is
authorized before that attribution.
