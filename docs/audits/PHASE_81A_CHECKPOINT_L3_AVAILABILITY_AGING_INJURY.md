# Phase 81A Checkpoint L3 - Availability, Aging And Injury

## Decision

**GO.** The final locked `7 x 2` completed with exactly seven workers and every
local lifecycle, recovery, injury, substitution and reconciliation gate held.

## Canonical Population

- profile: `phase81a-availability-aging-l3-7x2`;
- seed prefix: `phase81a-availability-aging-l3-v1`;
- worlds/seasons/workers: `7 / 2 / 7`;
- team-match observations: `25,704`;
- player match-hours: `420,667.4667`;
- report: `simulation-out/phase81a-availability-aging-l3-7x2.json`.

## Final Measurements

| Measure | Result | Gate |
|---|---:|---:|
| time-loss match injuries | `9,101` | observed in `7/7` worlds |
| time-loss incidence | `21.6347/1000h` | `20..50` |
| mean substitutions | `3.827381` | `3.5..4.9` |
| season-one / season-two substitutions | `3.846250 / 3.808512` | diagnostic |
| median first substitution | `60` | `50..70` |
| unavailable starters selected | `0` | `0` |
| consequence/reconciliation/limit failures | `0 / 0 / 0` | `0 / 0 / 0` |
| recovery controlled bounds | held | held |
| generated veteran inversion | held | held |

Every age bucket had positive exposure and non-zero time-loss injury
reachability. No bucket's injury count approached its positive appearance
count. Longitudinal formation/stable-ID gates also held; the old L1 opening-XI
diagnostic remains reported but is not a lifecycle regression gate.

## Refinement Evidence

The first run measured only `6.0570/1000h`. A versioned `3.5 x` occurrence
policy reached the incidence band, but exposed a real 15-player roster with no
forward. The canonical catalog selector now uses the least-bad scored emergency
shape only when no ordinary shape can field eleven; it never recalls an
unavailable player or installs a fixed formation.

After injury reachability, substitution volume was `3.184718`. A third minute-60
evaluation changed it only to `3.186702` and was removed. Canonical reason facts
then attributed `51,616/53,219` no-legal outcomes to the quality floor, not
competition limits or missing positional cover. Moving only the low-condition
regression allowance from `2` to `3` reduced quality-floor failures to `34,081`
and produced the final `3.827381` without a limit violation. The full failed
sequence remains in Steps 06B5A and 06B5B; this audit records the accepted
result, not a rewritten first attempt.
