# Phase 81A Downstream Owner Replication

## Decision

`REFINE` before replay. The downstream owner is `not_evaluated`.

Both new tactical matrices pass canonical Phase 1, but one real club in one of
the `84` competition rows fields one weak positional fit. The frozen protocol
requires every population row to pass, so the profile correctly withholds all
chance-to-result attribution rather than simulating around a rejected
population.

## Population And Structural Result

The locked profile ran alone with exactly seven workers over two untouched
sets of `14` worlds. Each set produced `6,804` directed analytic contexts from
`756` real formation matchups and nine tactical responses.

| fact | set A | set B | gate |
|---|---:|---:|---:|
| effective signatures | `9/9` | `9/9` | complete |
| best-response signatures | `6/9` | `6/9` | `>= 0.25` share |
| ubiquity multiple | `3.5013` | `3.5026` | `<= 4` |
| material cycles | `292` | `298` | `> 0` |
| conservation mismatches | `0` | `0` | `0` |
| mirror mismatches | `0` | `0` | `0` |
| population held | no | yes | both yes |

The sole rejected row is
`phase81a-b2-downstream-replication-a-001 / competition:ita-2` with
`avoidable_out_of_position`. Its one offending club is `club:ita-2-01`, squad
identity `holder_heavy_low_build_up`, uniquely selecting `4-1-4-1` at
`161.74` versus `161.10` for the runner-up and fielding one weak fit.

The diagnostic artifact is
`simulation-out/phase81a-b2-downstream-replication.json`, SHA-256
`b7296090febb1c36fe456676452ee1b688f993db175c94c2ef64b0537d8d1fc2`.
The command returned real exit `1` after about `22.2s`; the expensive replay
was not run by protocol.

## Owner

The domain already owns the distinction:
`isCoveringSuitability(...)` accepts only `natural` and `adapted`; `weak` is a
displayed squad gap, not credible ordinary coverage. The free AI selector has
a private `isUsableSuitability(...)` that accepts every fit except `invalid`.
Its prose says it first searches only credible fits, but its duplicate predicate
allows `weak` into that ordinary search.

This is not a request to prohibit emergency football. The existing
`strongestEmergencyCatalogShape(...)` already retries the catalog with every
candidate when no credible XI exists. Step 06C12A removes the duplicate
ordinary predicate, consumes the domain owner and preserves that emergency
path. The exact failed club is the real-data reachability proof.

## Handoff

Step 06C12A owns only ordinary coverage semantics. After its targeted real-seed
proof and full gate, this same locked profile repeats unchanged. Only when all
population rows pass may its `64`-context replay name the downstream owner.
