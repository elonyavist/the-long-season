# Phase 81A Checkpoint L4.3 - Generated-Ceiling Attribution

## Decision

**OWNER IDENTIFIED: `downstream_selection_or_outcome`.** The v9 revalidation
classifies `5/7` worlds downstream and `2/7` as development realization; all
twenty-one division rows have complete denominators. L5 remains `REFINE`.

## Evidence

- profile: `phase81a-generated-ceiling-l4-3-7x10`;
- population: `7` worlds, `10` seasons, `3` divisions, `7` workers;
- report: `simulation-out/phase81a-generated-ceiling-l4-3-7x10.json`;
- canonical report hash: `95232b66a9ef55f0d8371face4fb613a`;
- fresh cache: `facts-v2`, projection bundle v9;
- owner counts: development realization `2`, generation quality `0`,
  downstream selection/outcome `5`, unidentified `0`;
- denominator failures: `0`.

The annual intake's stored role-potential P90 still exceeds the corresponding
opening-senior current median. In first division the seven pairs are potential
`15.03..15.20` versus opening `13.08..13.21`; mature current P90 is now
`11.73..12.62`. At least two divisions clear the mature-current comparison in
five worlds, moving the cohort owner downstream after the development repair.

The `21` `(world, division)` rows are byte-identical to the independently
retained L5 generated-ceiling rows (SHA-256 `ab94c90c...7c62f`). This freezes
the new L4.3 input signature `6ef3c51a7717bb39ff86e66be6629848`
without treating L5's own output as its baseline.

## Consequence

Generation quality and development realization are no longer the demonstrated
cohort owner. The remaining renewal deficit belongs to downstream selection or
outcome, while L5 separately leaves late leader age and formation replication
red. No further owner is implemented before the requested HTML review.
