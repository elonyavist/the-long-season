# Phase 81A Checkpoint C - Player Context

## Verdict

**REFINE: product premise review.** Player context, squad identity, analytic
diversity, original dominance and blind neutrality all hold. The sole red is
the unchanged `+0.045/-0.045` adversarial counter-move target. Steps 07-08 are
not reopened and match-result variance is not reduced.

## Frozen Run

```text
code before report-only changes: 23a9596
profile: phase81a-c
population: 14 A2 worlds + 28 untouched downstream worlds
workers: exactly 7
artifact: simulation-out/phase81a-checkpoint-c-player-context.json
sha256: 4605f3aba8965de954b0fda85c258f5b9b9764cdcf2677a77ed86a7aad0bd766
real exit: 1
```

The profile composes existing canonical producers. It does not regenerate an
alternative population, cache an older report, or add a second simulator.

## Player Context

| Gate | In-sample | Out-of-sample | Target |
| --- | ---: | ---: | ---: |
| top formation share | `0.1270` | `0.1349` | `<= 0.50` |
| distinct formations | `14` | `11` | `>= 6` |
| reachable primary roles | `10/10` | `10/10` | `10/10` |
| modal shapes across identities | `8` | `8` | `>= 3` |
| catalog reorder invariance | `1.0000` | `1.0000` | `1.0000` |
| mean out-of-position slots | `0.0000` | `0.0000` | `<= 0` |
| identities observed | `8/8` | `8/8` | `8/8` |

The low-block guardrails also hold in both A2 populations: conceded-xG
reduction is `0.2395/0.2633` and own-loss per conceded reduction is
`0.8944/1.4301`.

## Structural And Result Evidence

Both untouched `14`-world sets retain all `9` effective tactical signatures,
response-diversity share `6/9`, ubiquity multiples `3.1971/3.1601`, material
cycles and `PASS_PHASE_1`. The three frozen dominance readers pass; their
observed values remain `0.4062`, `0.5180` and `0.5141`.

Blind deltas remain neutral at `+0.00165/-0.00196`, with both 95% intervals
crossing zero. The optimistic player-aware response arms reach only
`+0.02258/-0.01832` and `+0.02162/-0.01629`, below the frozen
`+0.045/-0.045` target. The downstream owner is consistently
`result_resolution`; the prior causal decomposition already rejected reducing
football variance to manufacture a larger tactical result delta.

## Product Consequence

The target is not lowered after output. It remains valid evidence that the
current engine does not deliver a `4.5` percentage-point opponent-specific
counter-move. It is moved to the future opponent-aware manager/AI contract.

The active MVP has a different, previously accepted boundary: AI selects its
best formation and tactical prior from its own squad, availability, condition
and match state; it does not consume opponent reads. The next step must freeze
an own-squad/season-level agency contract before measuring it. It may not use
the observed `0.02258` as its new threshold, and it may not reopen Steps 07-08.

## Renewal Boundary

Checkpoint C changes and measures no generation, development, market, aging,
availability or minute policy. L6.31 therefore remains the latest focused
renewal evidence, not the final proof. The integrated `7 x 10` closeout must
rerun its ready-replacement and generated-leader facts under the completed
tactical engine.
