# Phase 81A - Integrated Option-B Closeout Cohort

## Decision

**Checkpoint F: `GO`.** The current tactical invariants pass, the fresh
option-B `7 x 2` career report rebuilds byte-identically, the integrated L6.31
renewal improvement remains green in both seed sets, and current-schema
tactical persistence is exact.

This is a `GO` for the smaller option-B product accepted by Amendments A11 and
A12. It is not a retroactive pass for the original counter-move, season-point
materiality or post-match preparation targets.

## Evidence Lanes

| Lane | Population | Result |
| --- | --- | --- |
| tactical invariants | canonical `phase81-tactical-shape`, one indivisible analytic job | `PASS`, hash `8f9c47610a59aea2a6a641d92363a81a` |
| option-B continuity | fresh `7 x 2`, three divisions, `25,704` team-matches, exactly `7` workers | `PASS`, hash `3bd5cc6f8ae576eaf875fb2d494541a8` |
| renewal | four paired current-engine L6.31 `7 x 10` arms, in-sample and OOS | `GO` in both sets |
| persistence | focused engine, JSON, real SQLite and web-runtime round trips | exact, `89/89` focused tests |

The analytic profile sees the whole tactical-shape matrix but not career AI or
human enjoyment. The E population sees two-season availability, selection,
live reactions and rotation but no human subjective experience. L6.31 sees
ten-season renewal but not Phase 81B replacement semantics or Phase 81C
contracts/background
fixtures. The persistence suite sees current-schema identity, not old beta-save
compatibility.

## Current Tactical Invariants

The isolated analytic run wrote
`simulation-out/phase81a-f-tactical-shape.json`. Its complete report decision is
`PASS`; all eight canonical invariants pass.

| Invariant | Observed | Gate |
| --- | ---: | --- |
| no dominant composition | worst matchup of strongest candidate `0.4062` | `<= 0.55` for every matchup |
| no dominant formation | maximum row mean `0.5180` | `<= 0.55` |
| no dominant tactic | maximum row mean `0.5141` | `<= 0.55` |
| bounded structural swing | `0.1025` of a division-tier edge | `<= 0.75` |
| incoherence cost | `2.3576` division-tier edges | `>= 1.0` |
| quality hierarchy with extreme shape | contender share `0.9721`, `21` upsets | `>= 0.55` and at least one upset |
| empty-department possession clamp | `0.18` floor | inside `0.18..0.82` |
| equal-strength shape distinction | all `3/3` pairs differ | all differ |

The `21` upsets are important product evidence: even with a large quality gap,
the weaker side can still win. Quality is decisive over a large sample without
making individual matches predetermined.

The analytic job correctly uses one worker because it is one indivisible
matrix; the seven-worker rule does not authorize fake or empty shards. Its
approximately ten-minute wall clock makes it a closeout gate, not a quick
canary.

## Option-B Continuity And Determinism

The locked `phase81a-e-option-b-continuity-7x2` profile was rebuilt from its
seven canonical `facts-v1` shards into
`simulation-out/phase81a-f-option-b-closeout.json`:

- process exit `0` and report decision `PASS`;
- effective worker count `7`;
- `cmp` against the original fresh Checkpoint E JSON: exit `0`;
- identical report hash `3bd5cc6f8ae576eaf875fb2d494541a8`;
- zero fallback, missing source, missing identity or reconciliation failure;
- maximum modal formation share `0.2222`;
- time-loss injuries `21.9529` per `1000` player-hours;
- mean substitutions `4.4007` per team-match and median first substitution at
  minute `60`;
- every accepted live reason reachable, with command rejection exactly `0`.

The pre-match policy remains own-squad only. Changing opponent identity alone
leaves the complete selection byte-identical; live AI may react to score, time,
fatigue, injury and dismissal.

## Longitudinal Renewal

The fresh current-engine L6.31 replay remains the ten-season baseline:

| Measure | In-sample delta | OOS delta | Intended direction |
| --- | ---: | ---: | ---: |
| stationary-ready share | `+0.1106` | `+0.1057` | `7/7`, `7/7` worlds |
| ceiling-gap reduction | `0.1648` | `0.1707` | `7/7`, `7/7` worlds |
| generated-leader share | `+0.0690` | `+0.0500` | `6/7`, `6/7` worlds |

Immediate potential-only purity is `144/144` in-sample and `147/147` OOS,
with zero structural or reconciliation failure and no new integrated red. This
is evidence of a material improvement, not a promise that every prospect
becomes a senior player.

## Persistence

Step 14 is the only coordinated Phase 81A storage schema/event-envelope reset.
Kickoff formation/focus, accepted tactical commands and resolver-owned shot xG
round-trip exactly through JSON, relational SQLite and the web runtime. Tactical
chapters are derived from those canonical facts after reload; no derived total
or guessed `4-4-2` default is stored.

## Accepted Limitations

- Checkpoint B's original response-diversity and ubiquity gates failed. That
  historical result remains visible and is not a nested failure of option B.
- The original `+0.045/-0.045` season-point materiality target was withdrawn.
  Option B accepts a smaller own-squad effect and does not relabel it.
- Post-match gameplay preparation is `not_implemented_in_mvp`; durable chapters
  explain the completed match only.
- Pre-match AI deliberately does not read the opponent. Opponent-aware planning
  requires a later visible-information and parity contract.
- Phase 81C still owns background fixtures, season-anchored contracts, the
  free-agent cycle and the final `750 x 10` world-integrity HTML.

## Residual Findings Outside This Decision

The closeout does not absorb older unowned findings: the young stored-ceiling
arrival-category gate, contract-finance integrity, preliminary-agreement
integrity, incomplete presentation-message families, incomplete role-department
ownership checks and the finite A6 absence enumeration. They remain recorded in
`docs/PROJECT_STATUS.md` with their original owners.

## Repository And Browser Verification

- `pnpm check`: exit `0`; `317/317` test files and `2545/2545` tests passed,
  with lint, dependency, localization, role/squad ownership,
  single-report-entrypoint and all package typechecks green;
- web production build: exit `0`, `2537` modules transformed;
- full serial Playwright visual QA: `38/38` passed in `7.3m`, including the
  current product journey and SQLite OPFS reset/current/future/rollback cases;
- one reduced-motion spinner assertion failed in the first serial run, passed
  unchanged in isolation, and the complete unchanged rerun passed. It remains
  the already-recorded timing-sensitive test, not product evidence;
- `pnpm depcruise` and `git diff --check`: exit `0`;
- local documentation links resolve and `PROJECT_STATUS.md` remains below its
  `300`-line hard limit;
- Graphify was rebuilt after the final documentation state.
