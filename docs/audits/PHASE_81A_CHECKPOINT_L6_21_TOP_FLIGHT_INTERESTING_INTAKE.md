# Phase 81A Checkpoint L6.21 — Top-Flight Interesting Intake

## Decision

`REFINE`. Remove the `+0.12` First-Division annual interesting-prospect
probability and all report code created only to measure it.

## Population

- seven exact L6.20 world seeds;
- ten seasons;
- exactly seven workers;
- L6.20 current arm read from its immutable cache;
- candidate arm simulated fresh;
- report SHA-256:
  `09b4962cf2028dcafd23bce719f93d7e1b46cdfd798d35564a41282d5cbbf18f`.

## Result

The candidate moved `189` First-Division prospects from routine to interesting,
held total volume, left both lower divisions unchanged and introduced no new
integrated failure. It nevertheless improved routine ceiling-failure share by
only `0.0371`, stored-ceiling failure by `0.0134`, and generated-leader share by
`0.0024`. Interesting leaders fell from `17` to `16`. World coherence also
failed every preregistered downstream target.

The candidate therefore changed labels more than outcomes. Frequency alone is
not a product solution.

## Handoff

L6.19 tested a bounded ceiling tail alone on another cohort; L6.21 tested
interesting frequency alone. Neither identifies whether their interaction is
material. L6.22 uses the same L6.20 seeds for a four-arm factorial comparison:
current, frequency only, ceiling only, and combined. If combined fails the
existing renewal targets, this whole generation-supply family closes and the
next owner must be outside prospect-class frequency and ceiling allocation.
