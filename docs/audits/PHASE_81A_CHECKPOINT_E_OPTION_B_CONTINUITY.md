# Phase 81A Checkpoint E - Option-B Multi-Match Continuity

## Decision

**GO.** The fresh `7 x 2` current-engine population passed every frozen
multi-match, live-reaction, formation, availability and reconciliation gate.
Focused current-schema tests also preserve exact tactical facts and chapters
through JSON, real SQLite and web runtime reload.

This decision does not claim post-match gameplay preparation. Amendment A12
records that feature as deferred for the option-B MVP.

## Population And Execution

- profile: `phase81a-e-option-b-continuity-7x2`;
- seed prefix: `phase81a-e-option-b-continuity-v1`;
- population: `7` worlds, `2` complete seasons, `3` divisions;
- observations: `42` competition-seasons and `25,704` team-matches;
- workers: exactly `7`;
- fresh cache: `facts-v1`, seven world shards;
- wall clock: approximately `83s` in the isolated command;
- report hash: `3bd5cc6f8ae576eaf875fb2d494541a8`;
- artifact: `simulation-out/phase81a-e-option-b-continuity-7x2.json`.

This population sees two-season load, recovery, injuries, suspensions,
selection, automatic substitutions and live score state. It does not measure a
human's subjective enjoyment or a post-match choice that the MVP does not
implement.

## Results

| Measure | Result | Frozen gate |
|---|---:|---:|
| time-loss injuries / 1000 player-hours | `21.9529` | `20..50` |
| mean substitutions / team-match | `4.4007` | `3.5..4.9` |
| median first substitution minute | `60` | `50..70` |
| unavailable selections | `0` | `0` |
| lifecycle/consequence mismatch | `0 / 0` | `0 / 0` |
| lineup/event/minute/limit failures | `0 / 0 / 0` | `0 / 0 / 0` |
| fallback/missing-source/missing-ID | `0 / 0 / 0` | `0 / 0 / 0` |
| formation-role retention | `42/42` | all green |
| maximum modal formation share | `0.2222` | `<= 0.30` |

All preregistered product reasons were reachable: forced injury `3,684`,
dismissal reorganization `2,238`, low condition `107,466`, poor performance
`652`, trailing response `47,485`, protecting lead `28,360`, no legal
substitute `23,205`, and no material change `21,698`. Command rejection was
exactly `0`.

Every age bucket had positive minutes and time-loss injury observations.
Controlled recovery bounds and generated-veteran resilience spread both held.

## Information And Persistence Proof

The existing generated-squad test now compares the complete selection under an
opponent-only identity change; formation, XI, bench, tactic and lateral focus
remain byte-identical. Production search finds no `OpponentRead`, formation
history or match-preparation reader in AI selection.

The focused persistence suite passed `89/89` tests. It covers required kickoff
formation/focus, accepted commands, resolver-owned xG, chapter derivation,
JSON, relational SQLite and web runtime reload. Step 14 remains the only Phase
81A storage schema/event-envelope reset.

## Limitations And Handoff

The accepted tactical season-point effect remains smaller than the historical
gate, and post-match preparation remains `not_implemented_in_mvp`. Neither is
hidden inside this GO. Step 16 may now run final integrated closeout on its
separately declared acceptance evidence.
