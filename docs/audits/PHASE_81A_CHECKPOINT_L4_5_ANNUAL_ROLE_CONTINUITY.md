# Phase 81A Checkpoint L4.5 - Annual Role Continuity

## Decision

**GO after the targeted retry.** The annual role owner remained green and Step
06B7G2A removed the separate match-day depth and catalog-order failure without
enlarging the generated senior roster.

## Evidence

The original locked `7 x 2 x 3` population produced canonical hash
`b38fe2ef541143af6e844d00891aec40`. Planned and generated candidates
reconciled `1,404/1,404`; role coverage, departments and sides all passed.
Across `42` competition-seasons, all ten senior roles, at least eight shapes,
top share `<= 0.2778`, zero fallback and zero reconciliation also passed.

That first run's failure was confined to season-one selections joined to opening identities:
`416/12,852` catalog-order-sensitive ties and `250` forced out-of-position
slots. Production has eleven academy players per club and a promotion ceiling
of `25`, but automatic selection sees only the 22 senior IDs. Step 06B7G2A owns
that boundary without generating extra senior incumbents.

The fresh retry produced canonical hash
`d9113b1687950a60870e724ad98a433c`, selected `3,234` real academy call-ups,
and recorded zero catalog-sensitive ties, zero fallback, zero avoidable invalid
slots and zero reconciliation failures. Forced emergency cover remained
observable (`33` invalid slots) and weak but legal cover remained observable
(`116` slots), so the green result did not make the failure branches
unreachable.

## Limitation

Two seasons prove structural continuity, not ten-season renewal. L4.6 and L5
retain ownership of development and leaderboard outcomes.
