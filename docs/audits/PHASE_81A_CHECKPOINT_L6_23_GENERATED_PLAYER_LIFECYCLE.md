# Phase 81A Checkpoint L6.23 — Generated-Player Lifecycle

## Verdict

`MIXED`. The rejected frequency-plus-ceiling arm changes many authored classes
but does not expose one lifecycle owner for its lower leader share.

## Population

- immutable L6.20 current and L6.22 combined caches;
- identical seven seeds, ten seasons and seven-worker metadata;
- accepted annual-academy IDs generated in seasons one through six;
- union join, so a one-arm acceptance is evidence rather than a dropped row;
- zero reconciliation failures;
- two byte-identical reports, SHA-256
  `88428f486bff8e32c729b67c8253865895b0eef84a614d1c0830486c22cb0ddc`.

## Result

| Fact | Count |
| --- | ---: |
| class-changed accepted players | `190` |
| leader losses | `59` |
| leader gains | `54` |
| net leader losses | `5` |
| quality-ready but not leader losses | `22` |
| mixed below frozen floors | `22` |
| exit/retention losses | `10` |
| current-profile losses | `5` |
| minute/development/intake losses | `0 / 0 / 0` |

The largest reason owns only `0.3729` of losses, below the preregistered
`0.50`, even though its loss-minus-gain sign is non-negative in six worlds.
The candidate therefore causes broad leader displacement rather than one
repairable terminal break.

## Instrument Corrections

Two false reader assumptions were corrected before any product conclusion.
One-arm IDs are valid intake-path divergences. Annual-academy origins also do
not persist opening current ability; the first canonical cached player-season
row is the honest baseline. The rejected draft had produced `11,370` false
reconciliation failures and was discarded, not interpreted.

## Consequence

No frequency, ceiling, growth, minute, retention or market rule opens from this
comparison. L6.24 returns to the current product and decomposes quality-ready
generated players by quality depth, selection volume, actor opportunity,
occasion conversion and top-ten cutoff. This distinguishes insufficient
population quality from match-output translation without an origin bonus.
