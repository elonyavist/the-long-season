# Phase 81A Checkpoint L6.8 - Prospective Succession

## Decision

`REFINE: target_eligibility`. Candidate rejected.

The first run (`c4a5f90b5f1531e4e74c3b797cfcf396`) stopped before gameplay
comparison because the historical L6.4 cache did not contain the renewal
episodes and population signatures required by the new reader. A fresh legacy
arm on the same seven seeds corrected the instrument without moving targets.

The corrected run (`bf91009d4580f2903d61e0a6c77012ab`) reconciled all worlds
and produced byte-equivalent control and candidate career outcomes. Both arms
recorded `4,040` fulfilled succession episodes, `5,198` acquisitions,
`0.087912` local replacement and `0.259524` career-generated season-ten leader
share. Prime-age acquisition, generated prime-age acquisition and every paired
downstream delta were zero.

The age-18-25 public-P50 tier is reachable in real generated inputs but does not
create a distinct live career choice at its half-point floor. It is removed;
the finding does not authorize hidden potential, forced youth selection,
expanded transfer capacity or a post-output threshold change.
