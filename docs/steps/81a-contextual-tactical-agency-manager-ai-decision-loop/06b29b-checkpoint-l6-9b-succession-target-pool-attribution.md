# Step 06B29B - Checkpoint L6.9B Succession Target-Pool Attribution

## Status

Done - `OWNER_IDENTIFIED: affordability`; split by 06B29C before correction.

## Question

L6.8 and L6.9 changed zero career decisions despite two distinct public P50
horizons. Which earliest canonical market filter prevents an affordable,
prime-age exact-role successor from reaching the generic ranking?

## Frozen Observation

For every permanent `role_succession` target search, record exactly one terminal
stage from this ordered funnel:

1. no exact-role candidate;
2. no age-18-29 candidate;
3. all such candidates already have a live talk;
4. seller squad/department safety removes them;
5. willingness rejects them;
6. no commercial snapshot is available;
7. transfer/wage affordability removes them;
8. candidates survive but none reaches the public P50 rebuilding floor;
9. a qualified candidate already wins the generic score;
10. a qualified candidate loses the generic score.

The observer may read the same public facts as selection but cannot change a
candidate, score, filter, offer, RNG draw or market limit. The exhaustive stage
vocabulary is shared from the engine; missing categories cannot disappear from
the report.

## Cohort And Decision

- fresh profile `phase81a-succession-target-pool-l6-9b-7x10`;
- `7` worlds, `10` seasons, exactly `7` workers;
- current product, no gameplay arm and no cached pre-instrumentation facts;
- `OWNER_IDENTIFIED` requires one stage to own at least `50%` of observations;
- `MIXED` records no sole owner and authorizes no correction;
- `NOT_OBSERVED` or any reconciliation failure is `STOP_RETHINK`.

The 50% rule is an attribution gate, not a gameplay balance target. Its branch
must be reachable on real generated data before closeout.

## Expected Files

- `packages/engine/src/career/ai-market-lifecycle.ts`, `index.ts` and tests;
- `apps/cli/src/commands/simulation-report/career-sections.ts`, tests,
  `report-registry.ts`, planner tests;
- `packages/i18n/src/labels.ts`;
- this document, resulting audit/index, Phase README and status.

No content, domain, persistence, web, match, growth, aging, intake, retirement,
lineup, finance, formation or HTML behavior changes.

## Outcome

Report hash `76fc1f424327d5cdb7e7d5c5823c1f4a`; `15,740` observed
searches. The combined economic stage owns `8,133` (`51.67%`), followed by
`4,626` qualified prime-age options losing the generic score (`29.39%`) and
`2,619` already winning (`16.64%`). Public quality (`283`), seller safety
(`69`) and willingness (`10`) are minor; exact-role supply is never absent.

Because the owner combined fee capacity and contract terms, no product change
is authorized yet. 06B29C splits those two facts on a fresh cohort.
