# Step 16G - Shared Reachable-Potential Policy And Paired Checkpoint

## Status

Done - **`STOP / RETHINK`**. The shared owner removed the diagnosed premature
compression, but it reduced the realised successor tail and made both quality
leaderboards older. The candidate and every analysis seam are removed at
closeout; no gameplay from this step ships.

## Entry Gate

Step 16F/L6.37 identified `premature_potential_compression`. Content constructs
young ceilings with one age/family reachability table; monthly engine aging
uses a second stricter table. In the seven current-product worlds, all `33/33`
represented First-Division serious prospects authored at ceiling 16 or better
lose more than 0.5 ceiling before their first senior observation, with mean
loss `2.47`; none reaches current quality 16.

L6.35 and L6.36 already reject a generic development multiplier, a higher tail
by itself, automatic U23 minutes and direct late-career physical rescaling.
Those branches do not reopen.

## Product Contract

Create one domain-owned pure function for remaining reachable ability room.
Content potential construction and engine potential compression must both call
it. Delete `familyGrowthCap(...)`, `remainingReachableRoom(...)` and
`goalkeeperRemainingRoom(...)` as independent policy owners.

The shared table is the already-shipped constructive reachability schedule; no
cell is selected from L6.38 output:

| family | <=17 | 18-21 | 22-24 | 25-27 | 28-31 | 32+ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| goalkeeper attributes, goalkeeper role | `10` | `9` | `4` | `3` | `2` | `1.2` through 34, then `0.3` |
| physical | `10` | `9` | `1.8` | `0.6` | `0.2` | `0` |
| mental | `10` | `9` | `3.5` | `2.2` | `1.4` | `0.6` |
| technical/other | `10` | `9` | `2.8` | `1.2` | `0.6` | `0.2` |

Goalkeeping attributes on an outfield role retain room `0`; role hard caps
remain final. The runtime evaluates current completed age, current ability and
the already-authored ceiling. It may only preserve or compress potential; it
never increases the generation-time profile and never changes current ability
directly.

Nothing else changes:

- positive growth still requires canonical minutes and keeps the existing age,
  performance, environment, role relevance, deterministic variance and `0.27`
  monthly bound;
- no player receives a goal, assist, selection, opportunity, transfer or
  valuation rule from age, origin or hidden ceiling;
- candidate volume, opening state and generation-time current/potential
  profiles are identical before the first monthly development checkpoint;
- the national exceptional-stock allocator and public assessment retain their
  existing owners.

## Analysis Control And Removal Rule

The paired run temporarily carries a typed `legacy`/`shared` compression mode
through the existing career advancement and `simulation-report` boundaries.
The legacy function is an analysis reference, never a product default and
never a generation caller.

After the verdict:

- `GO`: shared becomes the only runtime policy; legacy mode, switch, profile
  arm and tests disappear in this step;
- `REFINE` or `STOP / RETHINK`: candidate mode and shared runtime caller
  disappear; content keeps its existing construction semantics and the next
  step must reconsider the owner;
- no rejected callable profile, i18n label, optional flag or unused export may
  remain.

## Frozen L6.38 Population

- profile: `phase81a-shared-reachable-room-l6-38-7x10`;
- fresh prefix: `phase81a-shared-reachable-room-l6-38-oos-v1`;
- two paired arms, `legacy` and `shared`;
- seven worlds, ten seasons, exactly seven workers;
- separate fresh resumable cache identity per arm;
- diagnostic JSON first; HTML only after an adopted product passes the later
  integrated checkpoint.

Both arms must have identical opening IDs, roles, ages, clubs, contracts,
current profiles, potential profiles and RNG state. The first divergence must
be a potential-only monthly compression fact for the same player/month. Any
earlier or unrelated divergence is `STOP / RETHINK`.

## Frozen Measures

The causal cohort is every accepted First-Division academy player generated in
seasons 1-6 with generation-time role potential `>=16`. Report serious and
rare separately; never merge them to hide an empty lane. Player-season joins
use stable IDs and common support, and also report arm-only representation.

The shared factor is **material** only when all hold:

1. serious first-senior mean ceiling loss falls by at least `1.0`, and the
   share losing more than `0.5` falls by at least `0.50`;
2. the number of career-generated players that reach current quality 16 is at
   least `1.50x` a positive legacy count;
3. season-ten career-generated scorer/creator leader share improves by at
   least `+0.10`;
4. scorer and creator quality-rung mean ages each fall by at least `0.50`, and
   their age-33-plus shares each fall by at least `0.06`;
5. current-16, leader-share and both quality-age directions hold in at least
   `5/7` paired worlds;
6. at least 21 common-support serious ceiling-16 players and positive rare
   support make the compression measures non-vacuous.

These floors represent roughly two additional generated leaderboard players
per world and about one fifth of the current age gap. They authorize a real
structural improvement, not completion.

## Absolute Product And Guardrail Gates

The frozen final bands do not move:

- scorer mean age `25.5..28.5`, creator `25.0..28.5`;
- scorer and creator age-33-plus shares each `<=0.12`;
- generated season-ten leader share `>=0.50`;
- an age-33-plus output leader remains reachable in at least `3/7` worlds.

Every previously green integrated, ability-range, current<=potential,
exceptional-stock, economy, standings, substitutions, injuries, formation,
tactical and reconciliation gate must remain green. Existing unrelated reds
remain visible and give the candidate neither credit nor blame. No new failure
is allowed.

Decision is total:

- `GO_CLOSE` when materiality, coherence, all absolute bands and guardrails
  hold;
- `GO_MATERIAL_CONTINUE` when the factor is material/coherent and regression-
  free but an absolute band remains red; adopt the one-owner repair, then
  attribute only the residual;
- `REFINE` when reachability/purity are valid but materiality or coherence
  fails;
- `STOP_RETHINK` for purity, reconciliation, invariant, veteran-reachability,
  new-gate failure or directional reversal.

No coefficient or threshold may change after reading L6.38.

## Expected Files

- `packages/domain/src/player/player-development-reachability.ts` and test
  **(new)**, player barrel: the one total age/role/family owner;
- `packages/content/src/generators/player-potential-allocation.ts` and focused
  tests: consume the owner and remove its private table;
- `packages/engine/src/career/player-aging-policy.ts` and focused tests: consume
  the owner; temporary legacy analysis reference has this step as removal owner;
- `packages/engine/src/index.ts`: expose the temporary typed analysis mode and
  canonical monthly fact through the package's existing public surface; it is
  removed with the paired control after the verdict;
- existing monthly/season career advancement and tests only where the typed
  analysis mode must cross the runtime boundary;
- `apps/cli/src/commands/simulation-report/reachable-potential-attribution.ts`
  and test **(new)**: the focused paired purity, common-support, materiality and
  total-decision reader. Keeping this temporary evaluator out of the already
  broad career-section orchestrator makes its removal after the verdict
  mechanical rather than leaving analysis-only branches behind;
- `apps/cli/src/commands/simulation-report/` registry, planner, career facts,
  renewal attribution and focused tests: two arms, purity, common-support
  funnel, decision and report;
- `packages/i18n/src/labels.ts`: temporary visible profile label, removed at
  closeout with the callable profile;
- `apps/cli/src/commands/simulation-report/career-sections.test.ts` and
  `locked-profile-sections.test.ts`: remove every remaining call-site timeout
  override after both files passed alone (`20/20` and `3/3`). They now read the
  already-measured `360_000` repository budget owned by
  `vitest.config.ts`, matching the same cleanup already applied to
  `career-world-facts.test.ts`; no new timeout number is introduced;
- `packages/content/src/generators/domestic-world.test.ts` and
  `player-prospect-joint-profile.test.ts`: the repository-wide census found the
  only two remaining call-site timeout overrides outside the report suite.
  Removing them completes, rather than approximates, the same single-owner
  budget correction;
- this document, Step 16F, phase README, audit index, status and the generated
  L6.38 audit.

Before touching any additional file, add it here with the ownership reason.
No save/schema/beta reset is needed; beta compatibility is not preserved, but
this change adds no durable field.

## Required Checks

Focused domain/content/engine/report tests, typecheck, dependency boundaries,
localization and diff check. Then the checkpoint runs alone:

```sh
pnpm cli simulation-report \
  --profile=phase81a-shared-reachable-room-l6-38-7x10 \
  --workers=7 --format=json \
  --report-output=simulation-out/phase81a-shared-reachable-room-l6-38-7x10.json
```

After the verdict, remove the rejected arm and every analysis seam, refresh
Graphify and run `pnpm check` alone. A fresh integrated product `7 x 10` and
desktop HTML are permitted only after an adopted branch exists.

## Recorded Result

The fresh paired `7 x 10` ran alone with exactly seven workers. Opening states
were identical and the first difference was potential-only in all `7/7`
worlds. A pre-execution reader assumption was corrected before the cached
rebuild: the two age tables cross after age 21, so purity requires that only
potential facts differ, not that the candidate always compress less in every
later month. The corrected reader is pinned by a focused test and changed no
gameplay fact, target or decision.

| measure | legacy product | shared owner | required delta |
| --- | ---: | ---: | ---: |
| serious first-senior mean ceiling loss | `2.9191` | `0.0052` | reduction `>=1.0` |
| serious share losing `>0.5` | `1.0000` | `0.0000` | reduction `>=0.50` |
| generated players reaching current 16 | `17` | `7` | multiplier `>=1.50` |
| generated season-ten leader share | `0.2357` | `0.2214` | delta `>=+0.10` |
| scorer quality-rung mean age | `31.3286` | `32.1000` | reduction `>=0.50` |
| creator quality-rung mean age | `31.3286` | `32.1000` | reduction `>=0.50` |

Compression therefore held, while current-16, leader, quality-age, direction
and absolute gates failed. Directions held in only `1/7`, `2/7`, `1/7` and
`1/7` worlds respectively. The decision is the preregistered
`STOP / RETHINK`, not a threshold adjustment.

The paired population explains the reversal. The same First-Division intake
contains `63` legacy rare prospects but only `25` under the truthful shared
stock. Preserved age-15-to-20 six-star ceilings occupy the national exceptional
stock until they age out; the old compression had been manufacturing vacancies.
The shared arm improves serious prospects' mean maximum current quality from
`12.1251` to `13.0373`, but none reaches 16. At ages 20-21 their mean current
quality is only about `12.7` against preserved potential `16.2`: removing the
false ceiling deletion does not supply the missing football-to-quality
conversion. The full evidence is in
[`PHASE_81A_CHECKPOINT_L6_38_SHARED_REACHABLE_POTENTIAL.md`](../../audits/PHASE_81A_CHECKPOINT_L6_38_SHARED_REACHABLE_POTENTIAL.md).

The first full closeout gate recorded `2543/2545` passing tests and two timeouts
only: a 60-second resume replay completed in 62.4 seconds and a 20-second live-
match replay completed in 22.0 seconds. Both whole files passed alone (`20/20`
in 310.7 seconds; `3/3` in 17.9 seconds). A second full gate reached
`2544/2545` and exposed one more 60-second override in the same career file at
65.4 seconds. The remaining call-site timeouts in those two report files were
therefore removed, then a repository-wide census removed the only two remaining
content-test overrides. The single global budget is now authoritative. This is
a suite-budget correction, not gameplay evidence.
