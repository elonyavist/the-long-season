# Step 06B27C - Checkpoint L6.7B Physical Aging 7 x 10

## Status

Done - `REFINE: owner_not_sufficient`; candidate rejected.

## Frozen Population And Gates

Same seven L6.4 worlds, ten seasons and seven workers. Profile
`phase81a-physical-aging-l6-7b-7x10` uses a fresh cache. L6.4 remains the frozen
control. Every L6.7 gate and threshold remains unchanged; nothing is relaxed
after the first REFINE.

Additionally, the second candidate must outperform L6.7 on the causal
trajectory and both late-career leader shares. If it moves only selection while
the trajectory remains positive, the decision is `REFINE:
downstream_selection`; if exceptional veterans disappear or opening-senior
survival falls below `0.30`, it is `STOP_RETHINK`.

## Expected Files

- report registry/planner and five-language labels for the locked profile;
- this step, audit/index, phase README and status.

No new gameplay tuning after reading this output. A second curve miss closes
curve calibration and assigns the remaining owner from the observed path.

## Outcome

- trajectory `+0.542944` baseline, `+0.503535` L6.7, `+0.502327` L6.7B;
  all remain positive in `7/7` worlds;
- generated leader share `0.259524 -> 0.271429`, below both absolute and delta
  gates;
- scorer age-33-plus share `0.466667 -> 0.384127`, but assist share only
  `0.387302 -> 0.349206` and worsens versus L6.7;
- opening-senior leader-slot share worsens `0.664286 -> 0.700000`;
- survival `0.518639`, exceptional observations `462`, formation retention
  `0.876190`, zero unknown/reconciliation failures.

The curve is not a sufficient causal owner. The shipped policy is restored.
The live alternative-share facts (`0.03185` quality-matched younger;
`0.01570` also fresher) and L6.6's absent ready target identify prospective
succession recruitment as the next bounded question.
